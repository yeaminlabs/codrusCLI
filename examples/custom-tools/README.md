# Example: Custom Tools

This example demonstrates how to write custom tools for CodrusCLI powered by Codrus models and add them to your agent spec file.

```sh
cd examples/custom-tools
uv sync --reinstall
uv run main.py
```
